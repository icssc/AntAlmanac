export interface Friend {
    id: string;
    name?: string;
    email: string;
    avatar?: string;
}

export interface FriendRequest {
    id: string;
    name?: string;
    email: string;
    avatar?: string;
}
