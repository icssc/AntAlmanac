<?xml version="1.0" encoding="utf-8"?>
<!--
  Mirrors apps/pwa/src/AntAlmanac/Info.plist.

  Key parallels:
    iOS Info.plist                     -> AndroidManifest equivalent
    CFBundleIdentifier                 -> package / applicationId (build.gradle.kts)
    CFBundleDisplayName                -> @string/app_name
    LSApplicationCategoryType          -> appCategory="productivity"
    UIBackgroundModes (remote-notif)   -> RECEIVE intent + FCM service
    UISupportedInterfaceOrientations   -> screenOrientation="fullSensor"
    WKAppBoundDomains                  -> assetlinks.json + autoVerify links
    aps-environment (Entitlements)     -> POST_NOTIFICATIONS permission
    associated-domains: applinks       -> data android:host="antalmanac.com"
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Mirrors iOS NSCameraUsageDescription / NSMicrophoneUsageDescription:
         requested only when the web app calls getUserMedia. Marked optional
         so devices without these features (tablets, Chromebooks) still
         install the app. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Mirrors iOS NSLocationWhenInUseUsageDescription. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Android 13+ runtime permission for displaying notifications.
         iOS's aps-environment entitlement covers this implicitly. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".AntAlmanacApp"
        android:allowBackup="true"
        android:appCategory="productivity"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AntAlmanac"
        android:usesCleartextTraffic="false"
        tools:targetApi="34">

        <!-- Main launcher activity. Equivalent to the iOS SceneDelegate +
             ViewController pair: hosts the WebView and handles incoming
             intents (launcher tap, App Link, shortcut). -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:screenOrientation="fullSensor"
            android:theme="@style/Theme.AntAlmanac.SplashScreen"
            android:windowSoftInputMode="adjustResize">

            <!-- Standard launcher entry. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!--
              App Links: claims https://antalmanac.com/* so universal-link-style
              navigation routes here instead of Chrome. Mirrors the iOS
              `com.apple.developer.associated-domains` entitlement +
              applinks:antalmanac.com declaration.

              autoVerify="true" tells the system to fetch assetlinks.json
              (served by apps/antalmanac/src/app/assetlinks.json/route.ts)
              and verify the package + SHA-256 fingerprint match before
              activating the filter. Until verification succeeds, the system
              still shows a chooser; once verified, links open in-app
              automatically.
            -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="antalmanac.com" />
            </intent-filter>
        </activity>

        <!--
          FCM bridge service. Mirrors the MessagingDelegate +
          UNUserNotificationCenterDelegate extensions of AppDelegate.swift.

          Disabled until google-services.json is committed and the
          firebase-messaging dependency is uncommented in build.gradle.kts.
        -->
        <service
            android:name=".AntAlmanacMessagingService"
            android:enabled="false"
            android:exported="false"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
          Default FCM notification channel + icon. Mirrors the iOS notification
          presentation options chosen in AppDelegate.swift
          (`willPresent` -> [.banner, .list, .sound]).
        -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="@string/default_notification_channel_id" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
    </application>

    <!--
      Required on Android 11+ to query whether Chrome/another Custom Tabs
      provider is installed (used by MainActivity.startAuthSession). Without
      this, PackageManager queries return empty on API 30+.
    -->
    <queries>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
    </queries>

</manifest>
