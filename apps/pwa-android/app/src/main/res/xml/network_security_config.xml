<?xml version="1.0" encoding="utf-8"?>
<!--
  Lock the WebView to HTTPS for the domains we allow. Mirrors iOS
  NSAppTransportSecurity defaults (NSAllowsArbitraryLoads stays true on iOS
  only because some auth subresources legitimately use mixed-content paths
  that ATS would otherwise block — Android's WebView doesn't need the
  blanket allow because we already scope navigation in MainActivity).
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">antalmanac.com</domain>
        <domain includeSubdomains="true">icssc.club</domain>
    </domain-config>
</network-security-config>
