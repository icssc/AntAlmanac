{
  "name": "antalmanac",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky install",
    "start:aa": "turbo start --filter=antalmanac",
    "start": "VITE_LOCAL_SERVER=true turbo start",
    "build:aa": "turbo build --filter=antalmanac",
    "build": "turbo build",
    "deploy:aa": "turbo deploy --filter=antalmanac",
    "deploy:cdk": "turbo deploy --filter=cdk",
    "deploy": "turbo deploy"
  },
  "workspaces": [
    "apps/*"
  ],
  "devDependencies": {
    "husky": "^8.0.3",
    "lint-staged": "^13.1.1",
    "turbo": "latest"
  },
  "packageManager": "pnpm@8.6.0",
  "engines": {
    "pnpm": "^8.0.0",
    "node": ">=16"
  }
}
