name: antalmanac

services:
    db:
        build:
            context: apps/antalmanac-scheduler/db
            dockerfile: Dockerfile
        ports:
            - '5432:5432'
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres
            POSTGRES_DB: antalmanac
