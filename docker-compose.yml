name: antalmanac

services:
    db:
        build:
            context: packages/db
            dockerfile: Dockerfile
        ports:
            - "5433:5432"
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres
            POSTGRES_DB: antalmanac
