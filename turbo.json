{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "start": {},
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "build/**"
      ]
    },
    "lint": {},
    "format": {"dependsOn": ["lint"]}
  }
}