{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "start": {},
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "build/**",
        "dist/**",
        "cdk.out/**"
      ]
    },
    "lint": {},
    "format": {"dependsOn": ["lint"]}
  }
}